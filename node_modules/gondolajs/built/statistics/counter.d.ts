export declare class Counter {
    counter: Map<string, number>;
    addOnce(action: string): void;
    getCount(action: string): number;
    getCountData(): Map<string, number>;
    clear(): void;
}
